{
  "name": "sp-invoicer",
  "version": "0.0.2",
  "description": "Super Productivity plugin for creating invoices from project tasks",
  "author": "James Thompson",
  "homepage": "https://github.com/1jamesthompson1/sp-invoicer",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "npm run build && vitest run",
    "test:unit": "npm run build && vitest run tests/unit.test.js",
    "test:integration": "npm run build && vitest run tests/integration.test.js",
    "test:watch": "npm run build && vitest"
  },
  "keywords": [
    "super-productivity",
    "plugin",
    "invoice"
  ],
  "license": "MIT",
  "devDependencies": {
    "jsdom": "^22.1.0",
    "vitest": "^1.6.0"
  }
}
